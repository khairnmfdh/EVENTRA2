<!-- res/drawable/bg_circle_radius.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- or specify color if needed -->
    <!-- Optional: add stroke if you want border -->
    <stroke
        android:width="2dp"
        android:color="#000000" />
    <!-- Optional: add padding or other attributes -->
</shape>